package com.example.data.database

object DBIndex {
    //    GlobalScope.launch {
//        val db by inject<MongoDatabase>()
//
//        val indexOptions = IndexOptions().unique(true)
//        db.schemeCollection().createIndex(Indexes.ascending(SchemeEntity::schemeId.name), indexOptions)
//
//    }
}